name: Security baseline
on:
  push:
    branches: [main, release/v3-20260919]
  pull_request:
permissions:
  contents: read
jobs:
  web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - run: npm audit --audit-level=high
      - run: npm run test:security
      - run: npm run typecheck
      - run: npm run lint
      - run: npm run build
      - run: npx playwright install --with-deps chrome
      - run: node scripts/run-e2e.mjs tests/e2e/web-security.spec.ts tests/e2e/auth.spec.ts tests/e2e/public-content.spec.ts tests/e2e/account-contacts.spec.ts tests/e2e/registration-flow.spec.ts
